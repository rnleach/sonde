[package]
name = "sonde"
version = "0.0.1"
authors = ["ClumsyCodeMonkey <clumsycodemonkey@gmail.com>"]

[dependencies]
sounding-base = "^0.1"
sounding-bufkit = "^0.1"
sounding-analysis = "^0.1"
error-chain = "^0.11"
cairo-rs = "^0.3"
glib = "^0.4"
gtk = { version = "^0.3", features = ["v3_10"] }
gdk = "^0.7"
lazy_static = "^1"
chrono = "0.4"
itertools = "^0.7.0"

[profile.release]
lto = true
panic = 'abort'
